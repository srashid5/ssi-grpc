{
  "name": "simple-grpc",
  "version": "1.0.0",
  "description": "Demo code that highlights gRPC based communication between client and server.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "google-protobuf": "^3.12.0-rc.1",
    "grpc": "^1.24.2",
    "grpc-tools": "^1.8.1"
  }
}
